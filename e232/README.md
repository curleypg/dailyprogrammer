Practice with python manipulating strings and using file open() and read().

solvePalindrome() takes a text file, removes non-alphanumeric characters and evaluates if the string equals itself backwards.
